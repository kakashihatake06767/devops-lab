pipeline {
    agent any

    stages {

        stage('Deploy To AWS App Server') {
            steps {
                sh '''
                ssh ubuntu@16.16.67.222 "
                cd ~/devops-lab &&
                git pull
                "
                '''
            }
        }

    }
}
