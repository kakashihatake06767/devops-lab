pipeline {
    agent any

    stages {

        stage('Repository Check') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

        stage('Docker Check') {
            steps {
                sh '''
                ssh dhrupal@192.168.109.52 "docker ps"
                '''
            }
        }

    }
}
