pipeline {
    agent any

    stages {

        stage('Deploy To App Server') {
            steps {
                sh '''
                ssh dhrupal@192.168.109.52 "
                cd ~/devops-labs/flask-mysql &&
                docker compose down &&
                docker compose up -d --build
                "
                '''
            }
        }

    }
}
